<script type="text/javascript">
var msj = "<%= notice %>";
  $(document).ready(function() {
     $("[name='my-checkbox']").bootstrapSwitch();
      $("[name='my-checkbox']").on('switchChange.bootstrapSwitch', function (event, state) {
        
    id = $(this).attr('id');
    var aler = updatefarmacia(id, state); 
      
});
  if(msj!='')
    swal(msj, "", "success")

});
</script>

<h1 class="text-center">Lista de Farmacias por verificar</h1>

<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Cedulajur</th>
      <th>Sucursal</th>
      <th>Direccion</th>
      <th>Correo</th>
      <th>Telefono1</th>
      <th>Telefono2</th>
      <th>Cant lab</th>
      <th>Verificado</th>
    </tr>
  </thead>

  <tbody>
    <% @farmacia.each do |farmacium| %>
      <tr>
        <td><%= farmacium.nombre %></td>
        <td><%= farmacium.cedulaJur %></td>
        <td><%= farmacium.sucursal %></td>
        <td><%= farmacium.direccion %></td>
        <td><%= farmacium.correo %></td>
        <td><%= farmacium.telefono1 %></td>
        <td><%= farmacium.telefono2 %></td>
        <td><%= farmacium.cant_lab %></td>
        <td> <% if farmacium.verificado == true %> 
             <input type="checkbox" id="<%= farmacium.id %>" name="my-checkbox" checked  data-on-text="Activado" data-off-text="Desactivado" data-on-color="success" data-off-color="danger">
         <% else %> 
            <input type="checkbox" id="<%= farmacium.id %>" name="my-checkbox"  data-on-text="Activado" data-off-text="Desactivado" data-on-color="success" data-off-color="danger">
        <% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


