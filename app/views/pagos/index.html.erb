<script type="text/javascript">
var msj = "<%= notice %>";
  $(document).ready(function() {
  if(msj!='')
    swal(msj, "", "success")

});
</script>
<h1 class="text-center"> <span class="label label-default">Lista pagos de Farmacia</span></h1>
<br>

  <div class="col-lg-3 col-md-3 col-xs-4 col-sm-4 col-md-offset-2 col-lg-offset-2">
   <input type="text" class="form-control"  placeholder="Nombre" name="nombre" id="nombre">
  </div>
 <div class="col-lg-2 col-md-2 col-xs-3 col-sm-3">
  <button type="submit" class="btn btn-success" onclick="busquedaPresentacion();"><span class="glyphicon glyphicon-search" aria-hidden="true"></span> Buscar</button>
  </div>
<div class="col-lg-7 col-md-7 col-md-offset-2 col-lg-offset-2  col-xs-12 col-sm-12">
<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>Farmacia</th>
      <th>Número comprobante</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @pagos.each do |pago| %>
      <tr>
        <td width="20%"><%= pago.nombre %></td>
        <td width="20%"><%= pago.numcomprobante %></td>
        <td width="15%"> <% if pago.verificado == true %> 

            <label><input type="checkbox"  value="" onclick="verificarPago(<%= pago.id %>, <%= pago.farmacium_id %>);" checked>  Verificado</label>
         <% else %> 

             <label><input type="checkbox" value="" onclick="verificarPago(<%= pago.id %>, <%= pago.farmacium_id %>);">  Verificado</label>
        <% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>
 <div class="col-lg-8 col-md-8 col-md-offset-4 col-lg-offset-4 col-xs-offset-4 col-sm-offset-4 col-xs-8 col-sm-8" >
        <%= will_paginate @pagos, renderer: BootstrapPagination::Rails %>
    </div>
