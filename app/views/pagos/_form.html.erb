<%= bootstrap_form_for(@pago) do |f| %>
  <% if @pago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pago.errors.count, "error") %> prohibited this pago from being saved:</h2>

      <ul>
      <% @pago.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-4 col-lg-4 col-md-offset-4 col-lg-offset-4  col-xs-6 col-sm-6">

 <div class="field">
    <%= f.number_field :numcomprobante , label: "Número de comprobante" %>
  </div>
  <div class="field">
    <%= f.text_field :monto , label: "Monto Colones" %>
  </div>
 
  <div class="field">
    <%= f.number_field :cantLab , label: "Cantidad Laboratorios"%>
  </div>
  
  <div class="actions">
 <%= f.submit class: 'btn btn-success ' %>
  </div>
  
<% end %>
</div>
